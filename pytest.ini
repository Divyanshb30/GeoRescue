[pytest]
testpaths = tests
filterwarnings =
    error::DeprecationWarning
    # Quicklooks and heatmaps are pictures, not layers - dropping the transform
    # is the point, so rasterio's "no geotransform" notice is expected here.
    ignore::rasterio.errors.NotGeoreferencedWarning
